// postfix evaluation
#include <string.h>

#include "tokenMat.h"
#include "stack.h"

int evaluate(TokenMat tokens);

int evaluate(TokenMat tokens) {
    // use a stack to evaluate each token
    Stack stack;
    for(int i = 0; i < tokens.size; i++) {

    }

}
